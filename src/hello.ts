export const hello = (name: string): string => {
    return `Hello, ${name}!!`
}


class Sample {
     log_arg = (arg: string) => {
        console.log(arg)
        console.log(arg)
        console.log(arg)
        console.log(arg)


        console.log(arg)
        console.log(arg)
    }

    log_arg_2(arg: string) {
        console.log(arg)
        console.log(arg)
        console.log(arg)
        console.log(arg)
        console.log(arg)
    }

}
